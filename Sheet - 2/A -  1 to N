#include <iostream>
#include <algorithm>
#include <cmath>
#include <array>
#include <string>
#include <cctype>
 
using namespace std;
int main() {
    long long x;
    cin >> x;
    for (long long i = 1; i <= x; i++)
    {
        cout << i << endl;
    }

    return 0;
}
