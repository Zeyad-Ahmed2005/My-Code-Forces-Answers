#include <iostream>
#include <cmath>
#include <array>
#include <stdio.h>
using namespace std;
int main()
{
    string a,b,c,d;
    cin >> a >> b;
    cin >> c >> d;
    string first[2] = {a,b};
    string second[2] = {c,d};
    
    if ( first[1] == second[1]){
        cout << "ARE Brothers";
    }
    else{
        cout << "NOT";
    }
    return 0;
}
